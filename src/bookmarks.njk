---
layout: layouts/base.njk
title: Bookmarks
description: A curated list of articles, tools, and resources I recommend
eleventyNavigation:
  key: Bookmarks
  order: 10
permalink: /bookmarks/
---

{% include "components/breadcrumbs.njk" %}

{% set totalCount = bookmarks.bookmarks | length %}
{% set categories = bookmarks.bookmarks | map('category') | uniq | sort %}
{% set sortedDates = bookmarks.bookmarks | map('date') | sort %}
{% set lastUpdated = (sortedDates | reverse)[0] %}

{% set pageTitle = title %}
{% set pageDescription = "Essays, tools, talks, and rabbit holes I keep coming back to. Saved for future me — shared for you." %}
{% set itemCount = totalCount %}
{% set itemLabel = "bookmarks and counting" %}

<div class="max-w-5xl mx-auto">
  {% include "components/page-header.njk" %}

  <div class="mt-4 text-xs uppercase tracking-wide text-gray-500 dark:text-gray-400">
    Last updated {{ lastUpdated | readableDate }} · {{ categories | length }} categories
  </div>

  <div class="mt-4 flex flex-wrap gap-2">
    {% for category in categories %}
      <a href="#{{ category | slugify }}" class="tag tag-interactive gap-2 px-3 py-1 font-semibold">
        {% feather "hash", class="w-3 h-3" %}
        {{ category }}
      </a>
    {% endfor %}
  </div>

  {% for category in categories %}
    {% set items = bookmarks.bookmarks | selectattr('category', 'equalto', category) | sort(attribute='date') | reverse %}
    {% set itemCount = items | length %}
    <section id="{{ category | slugify }}" class="mt-12">
      <div class="flex flex-wrap items-end justify-between gap-3">
        <h2 class="text-2xl font-semibold text-gray-900 dark:text-white">{{ category }}</h2>
        <span class="text-sm text-gray-500 dark:text-gray-400">{{ itemCount }} saved{% if itemCount != 1 %}s{% endif %}</span>
      </div>

      <div class="mt-5 grid gap-4 md:grid-cols-3">
        {% for bookmark in items %}
          {% set domain = bookmark.url | domainFromUrl %}
          {% set favicon = bookmark.url | faviconUrl %}
          {% set domainInitial = bookmark.url | domainInitial %}
          <article class="card card-hover h-full card-padding">
            <div class="flex gap-4">
              <div class="relative flex h-12 w-12 shrink-0 items-center justify-center overflow-hidden rounded-lg border border-gray-200 bg-gray-50 dark:border-gray-700 dark:bg-gray-800">
                <span class="pointer-events-none text-xs font-semibold uppercase tracking-wide text-gray-600 transition-opacity duration-200 dark:text-gray-200">{{ domainInitial }}</span>
                {% if favicon %}
                  <img
                    src="{{ favicon }}"
                    alt="{{ domain or bookmark.title }} logo"
                    loading="lazy"
                    decoding="async"
                    class="absolute inset-1 h-auto w-auto object-contain"
                    onload="this.previousElementSibling.style.opacity = '0';"
                    onerror="this.previousElementSibling.style.opacity = '1'; this.remove();"
                  />
                {% endif %}
              </div>
              <div class="flex-1 space-y-3">
                <div class="space-y-2">
                  <a href="{{ bookmark.url }}" target="_blank" rel="noopener noreferrer" class="inline-flex items-start gap-2 text-base font-semibold text-gray-900 transition-colors hover:text-amber-600 dark:text-white dark:hover:text-amber-400">
                    <span>{{ bookmark.title }}</span>
                    {% feather "external-link", class="w-4 h-4 mt-0.5 opacity-70" %}
                  </a>
                  {% if domain %}
                    <p class="text-xs uppercase tracking-wide text-gray-500 dark:text-gray-400">{{ domain }}</p>
                  {% endif %}
                </div>
                {% if bookmark.description %}
                  <p class="text-sm leading-relaxed text-gray-600 dark:text-gray-300">{{ bookmark.description }}</p>
                {% endif %}
              </div>
            </div>
            <div class="mt-4 flex flex-wrap items-center justify-between gap-3 text-xs uppercase tracking-wide text-gray-500 dark:text-gray-400">
              <span>Added {{ bookmark.date | readableDate }}</span>
              {% if bookmark.tags %}
                <div class="flex flex-wrap gap-1">
                  {% for tag in bookmark.tags %}
                    <span class="tag">#{{ tag }}</span>
                  {% endfor %}
                </div>
              {% endif %}
            </div>
          </article>
        {% endfor %}
      </div>
    </section>
  {% endfor %}

  <div class="mt-16 card card-padding">
    <h3 class="flex items-center gap-2 text-lg font-semibold text-gray-900 dark:text-white">
      {% feather "mail" %}
      Have a suggestion?
    </h3>
    <p class="mt-2 text-sm text-gray-600 dark:text-gray-300">
      Know of an article or resource that should be here? <a href="https://github.com/carteakey/carteakey.dev/issues" target="_blank" rel="noopener" class="font-semibold hover:underline accent-link accent-focus">Open an issue</a> or <a href="mailto:{{ metadata.author.email }}" class="font-semibold hover:underline accent-link accent-focus">drop me a note</a>.
    </p>
  </div>
</div>
