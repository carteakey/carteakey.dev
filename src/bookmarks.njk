---
layout: layouts/base.njk
title: Bookmarks
description: A curated list of articles, tools, and resources I recommend
eleventyNavigation:
  key: Bookmarks
  order: 10
permalink: /bookmarks/
---

{% include "components/breadcrumbs.njk" %}

{% set totalCount = bookmarks.bookmarks | length %}
{% set categories = bookmarks.bookmarks | map('category') | uniq | sort %}
{% set sortedDates = bookmarks.bookmarks | map('date') | sort %}
{% set lastUpdated = (sortedDates | reverse)[0] %}

<div class="max-w-5xl mx-auto">
  <h1 class="text-4xl font-bold text-gray-900 dark:text-white flex items-center gap-3">
    {# {% feather "bookmark" %} #}
    Bookmarks
  </h1>
  <p class="mt-4 text-gray-600 dark:text-gray-300 text-lg">
    Essays, tools, talks, and rabbit holes I keep coming back to. Saved for future me — shared for you.
  </p>

  <div class="mt-8 rounded-xl border border-amber-200 dark:border-amber-700 bg-amber-50/70 dark:bg-amber-900/20 p-6 text-sm text-amber-900 dark:text-amber-200 flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
    <div>
      <p class="font-semibold text-base">{{ totalCount }} bookmarks across {{ categories | length }} categories</p>
      <p class="mt-1 text-amber-900/80 dark:text-amber-200/80">Last updated {{ lastUpdated | readableDate }}</p>
    </div>
    <div class="flex flex-wrap gap-2">
      {% for category in categories %}
        <a href="#{{ category | slugify }}" class="inline-flex items-center gap-2 rounded-full bg-white/80 dark:bg-gray-900/40 px-3 py-1 text-xs font-semibold uppercase tracking-wide text-amber-900 dark:text-amber-100 hover:bg-white dark:hover:bg-gray-800 transition">
          {% feather "hash", class="w-3 h-3" %}
          {{ category }}
        </a>
      {% endfor %}
    </div>
  </div>

  {% for category in categories %}
    {% set items = bookmarks.bookmarks | selectattr('category', 'equalto', category) | sort(attribute='date') | reverse %}
    {% set itemCount = items | length %}
    <section id="{{ category | slugify }}" class="mt-12">
      <div class="flex flex-wrap items-end justify-between gap-3">
        <h2 class="text-2xl font-semibold text-gray-900 dark:text-white">{{ category }}</h2>
        <span class="text-sm text-gray-500 dark:text-gray-400">{{ itemCount }} saved{% if itemCount != 1 %}s{% endif %}</span>
      </div>

      <div class="mt-5 grid grid-cols-1 md:grid-cols-2 gap-5 items-start">
        {% for bookmark in items %}
          {% set domain = bookmark.url | domainFromUrl %}
          {% set favicon = bookmark.url | faviconUrl %}
          {% set domainInitial = bookmark.url | domainInitial %}
          <article class="bookmark-card relative overflow-hidden rounded-xl border border-gray-200/80 bg-white/95 p-5 sm:p-6 shadow-sm transition duration-200 hover:-translate-y-1 hover:border-amber-500/80 hover:shadow-lg focus-within:-translate-y-1 focus-within:border-amber-500/80 focus-within:shadow-lg dark:border-gray-700/80 dark:bg-zinc-900/80 dark:hover:border-amber-500/80">
            <div class="flex gap-4">
              <div class="relative flex h-11 w-11 shrink-0 items-center justify-center overflow-hidden rounded-xl border border-amber-900/15 bg-amber-100/40 text-[0.75rem] font-semibold uppercase tracking-wide text-amber-900/80 transition-opacity duration-200 dark:border-amber-500/25 dark:bg-amber-900/30 dark:text-amber-100">
                <span class="pointer-events-none transition-opacity duration-200 ease-out">{{ domainInitial }}</span>
                {% if favicon %}
                  <img
                    src="{{ favicon }}"
                    alt="{{ domain or bookmark.title }} favicon"
                    loading="lazy"
                    decoding="async"
                    class="absolute inset-[0.3rem] h-auto w-auto object-contain"
                    onload="this.previousElementSibling.style.opacity = '0';"
                    onerror="this.remove()"
                  />
                {% endif %}
              </div>

              <div class="flex flex-1 flex-col gap-3">
                <div class="flex flex-col gap-2">
                  <a href="{{ bookmark.url }}" target="_blank" rel="noopener noreferrer" class="inline-flex items-start gap-2 text-lg font-semibold text-gray-900 transition-colors hover:text-amber-600 dark:text-white dark:hover:text-amber-400">
                    <span>{{ bookmark.title }}</span>
                    {% feather "external-link", class="w-4 h-4 mt-1" %}
                  </a>
                  {% if domain %}
                    <p class="text-xs uppercase tracking-wide text-gray-500 dark:text-gray-400">{{ domain }}</p>
                  {% endif %}
                  {% if bookmark.description %}
                    <p class="text-sm leading-relaxed text-gray-600 dark:text-gray-300">{{ bookmark.description }}</p>
                  {% endif %}
                </div>

                <div class="flex flex-wrap items-center justify-between gap-3 text-xs text-gray-500 dark:text-gray-400">
                  <span class="uppercase tracking-wide">Added {{ bookmark.date | readableDate }}</span>
                  {% if bookmark.tags %}
                    <div class="flex flex-wrap gap-1">
                      {% for tag in bookmark.tags %}
                        <span class="rounded-full bg-gray-100/80 px-2 py-0.5 text-gray-700 dark:bg-gray-700/80 dark:text-gray-200">#{{ tag }}</span>
                      {% endfor %}
                    </div>
                  {% endif %}
                </div>
              </div>
            </div>
          </article>
        {% endfor %}
      </div>
    </section>
  {% endfor %}

  <div class="mt-16 rounded-xl border border-gray-200 dark:border-gray-700 bg-gray-50 dark:bg-gray-800 p-6">
    <h3 class="flex items-center gap-2 text-lg font-semibold text-gray-900 dark:text-white">
      {% feather "mail" %}
      Have a suggestion?
    </h3>
    <p class="mt-2 text-sm text-gray-600 dark:text-gray-300">
      Know of an article or resource that should be here? <a href="https://github.com/carteakey/carteakey.dev/issues" target="_blank" rel="noopener" class="font-semibold hover:underline accent-link accent-focus">Open an issue</a> or <a href="mailto:{{ metadata.author.email }}" class="font-semibold hover:underline accent-link accent-focus">drop me a note</a>.
    </p>
  </div>
</div>
