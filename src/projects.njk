---
layout: layouts/home.njk
title: Projects
templateClass: tmpl-post
eleventyNavigation:
  key: Projects
  order: 4
---
<h1>Projects</h1>
<div class="grid md:grid-cols-2 gap-5">
  {% for project in projects.projects %}
    <div class="p-4 border-2 border-gray-200 shadow-sm dark:border-gray-600 overflow-hidden flex flex-col">
      <a href=" {% if project.github %} {{ project.github }} {% elif project.url  %} {{ project.url }} {% endif %}">
        <h3 class="text-xl font-bold">{{ project.title }}</h3>
      </a>
      {{ project.description }}
      <ul class="flex flex-row list-none text-sm mx-0 px-0">
        {% for tag in project.tags %}<li class="bg-gray-200 dark:bg-gray-600 p-1 mx-2">{{ tag }}</li>{% endfor %}
      </ul>
    </div>
  {% endfor %}
  <div class="p-4 overflow-hidden items-center align-middle justify-center">
    <p>
      .. explore the rest on my
      <a href="https://github.com/carteakey?tab=repositories">github</a>
    </p>
  </div>
</div>
