---
layout: layouts/base.njk
title: Workouts
---
{% include "components/breadcrumbs.njk" %}


<div class="max-w-5xl mx-auto">
    <h1>Recent Workouts</h1>
    <div class="mt-4 intro-block">
        <p>Here are my latest activities from Strava.</p>
    </div>

    <div class="mt-8 grid gap-4 md:grid-cols-2">
        {% for activity in strava %}
            <article class="card card-hover card-padding">
                <header class="flex flex-col gap-2">
                    <a href="{{ activity.url }}" target="_blank" rel="noopener noreferrer" class="inline-flex items-center gap-2 text-lg font-semibold tracking-tight text-gray-900 link-accent dark:text-gray-100">
                        {{ activity.name }}
                        {% feather "external-link", class="w-4 h-4" %}
                    </a>
                    <div class="flex flex-wrap items-center gap-2 text-xs uppercase tracking-wide text-gray-500 dark:text-gray-400">
                        <span class="tag">{{ activity.type }}</span>
                        <time datetime="{{ activity.start_date | htmlDateString }}">{{ activity.start_date | readableDate }}</time>
                    </div>
                </header>

                <dl class="mt-4 grid grid-cols-2 gap-3 text-sm text-gray-600 dark:text-gray-300">
                    <div>
                        <dt class="uppercase tracking-wide text-xs text-gray-500 dark:text-gray-400">Distance</dt>
                        <dd class="font-medium text-gray-900 dark:text-gray-100">{{ activity.distance }} miles</dd>
                    </div>
                    <div>
                        <dt class="uppercase tracking-wide text-xs text-gray-500 dark:text-gray-400">Moving Time</dt>
                        <dd class="font-medium text-gray-900 dark:text-gray-100">{{ activity.moving_time }} minutes</dd>
                    </div>
                </dl>

                {% if activity.mapImageUrl %}
                    <div class="mt-4 overflow-hidden rounded-lg border border-gray-200 dark:border-gray-800">
                        <img src="{{ activity.mapImageUrl }}" alt="Map of {{ activity.name }}" loading="lazy" decoding="async" class="w-full object-cover" style="height: auto;">
                    </div>
                {% endif %}
            </article>
        {% endfor %}
    </div>
</div>
