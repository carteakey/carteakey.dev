---
layout: layouts/home.njk
title: Projects
templateClass: tmpl-post
eleventyNavigation:
  key: Projects
  order: 4
---

<h1>Projects</h1>

<p >
  Here are some of my personal projects. More can be found on my <a href = "https://github.com/carteakey?tab=repositories">github</a>.
</p>

<div class="grid md:grid-cols-2 gap-5">
  {% for project in projects %}
 
  <div
    class="project-card p-4 border-2 border-gray-200 shadow-sm dark:border-gray-600 overflow-hidden flex flex-col"
  >

  {# <div class="place-self-end float-right">
    {% if project.github %}
    <a href="{{ project.github }}">
      <button
        class="self-center font-bold p-1 sm:px-2 sm:py-2 hover:bg-gray-200 hover:dark:bg-gray-600 transition-all"
      >
        {% feather "github" %}
      </button>
    </a>
    {% endif %}
  </div> #}

    <a href=" {% if project.github %} {{ project.github }} {% elif project.url  %} {{ project.url }} {% endif %}">
      <h3 class="text-xl font-bold">{{ project.title }}</h3>
    </a>
    {{ project.description }}
   
    <ul class="flex flex-row list-none text-sm mx-0 px-0">
      {% for tag in project.tags %}
      <li class="bg-gray-200 dark:bg-gray-500 p-1 mx-2">{{ tag }}</li>
      {% endfor %}
    </ul>
   

    
  </div>
  {% endfor %}
  <div
    class="project-card p-4  overflow-hidden flex flex-col items-center align-middle justify-center"
  >
  .. more can be found on my <a href = "https://github.com/carteakey?tab=repositories">github</a>
  </div>
</div>
